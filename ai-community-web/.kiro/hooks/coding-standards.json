{
  "version": "v1",
  "hooks": [
    {
      "name": "遵循專案編碼規範",
      "trigger": "PreToolUse",
      "matcher": "fs_write|str_replace|fs_append",
      "action": {
        "type": "agent",
        "prompt": "確認寫入的程式碼遵循以下專案規範：\n1. JavaScript: 使用 const/let（不用 var）、camelCase 變數名\n2. HTML: 語意化標籤、包含 ARIA 屬性\n3. CSS: 使用 CSS 變數管理色彩\n4. API 回傳格式: { success: boolean, data: any, message: string }\n5. DynamoDB marshalled 格式: {S:'string'}, {N:'123'}, {L:[...]}\n6. 繁體中文 UI 文字\n如果違反規範，提出修改建議。"
      }
    }
  ]
}
